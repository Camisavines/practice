# 1
# INPUT: send a string
# RETURN: the reverse of that string
def reverseString(xstring):
    result = ""
    # TODO: finish the function
    return result
    
    
# 2
# INPUT: send a string
# RETURN: the number of times each letter appears in the word
def letterCount(xstring):
    counter = {}
    # TODO: finish the function
    return counter
  
  
# 3
# INPUT: send a string
# RETURN: True if the string is a palindrome otherwise return False
# a palindrome is a word that reads the same backward as forward
def palindrome(xstring):
    reversed = ""
    # TODO: finish the function
  
  
  
# 4
# INPUT: send a string
# RETURN: True if the string is a palindrome otherwise return False
# an isogram is a word without any repeating letters
def isogram(xstring):
    wordList = []
    # TODO: finish the function
  
  